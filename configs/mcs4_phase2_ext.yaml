# MCS exp4 — Phase 2 Braid Finetune (extended: epoch 41-50)
# matte cond: OFF  |  gate: ON (all blocks)
base: configs/phase2_braid.yaml

model:
  zero_matte_cond: true

training:
  schedule: all
  resume: checkpoints/mcs4_phase2/final.pth
  batch_size: 16
  epochs: 50

checkpointing:
  output_dir: checkpoints/mcs4_phase2/

wandb:
  tags: [mcs, mcs4, phase2, no-matte-cond, gate, ext50]
